<div class="panel panel-danger">
  <div class="panel-heading">
    <%= pluralize(errors.count, "error") %> prohibited this post from being saved:
  </div>
  <div class="panel-body">
    <ul>
    <% errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
</div>
